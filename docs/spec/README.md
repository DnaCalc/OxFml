# OxFml Spec Index

This directory is the OxFml-owned mutable spec set after bootstrap.

## Canonical spec ownership (post-bootstrap)
- `docs/spec/fec-f3e/*` (authoritative shared seam spec files)
- `docs/spec/formula-language/*` (formula-language concrete rules and conformance matrix)
- `docs/spec/formatting/*` (formula-semantic formatting clauses consumed by evaluator)

## Mirror policy
Foundation keeps read-only mirrors for conformance governance and cross-lane references.
