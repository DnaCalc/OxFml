[package]
name = "oxfml_core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
name = "oxfml_core"
path = "src/lib.rs"

[dependencies]
oxfunc_core = { path = "../../../OxFunc/crates/oxfunc_core" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
